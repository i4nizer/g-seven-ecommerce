<section id="products-section">
    <h2>Products</h2>
    <%- include('product-search-bar') %>

    <%# Render list of categories %>
    <% for (const index in categories) { %>
        <%- include('product-category', { key: index, category: categories[index] }) %>
    <% } %>
    

</section>

<style>
#products-section {
    width: 100%;
    height: fit-content;
    padding: 2dvw 0;

    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;

    color: var(--black);
    background-color: var(--white);
}

#products-section h2 {
    width: 100%;
    height: 60px;

    color: var(--red);
    font-size: 50px;
    text-align: center;
}
</style>