<div class="order-history-control-head">
    <%- include('order-history-tab-control') %>
    <%- include('order-history-date-range') %>
</div>
<style>
    .order-history-control-head {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 10px;
        width: 99%; /* Takes up the full width of its parent container */
        position: fixed; /* Stays fixed when scrolling */
        top: 60px; /* Adjust this value to match the height of your header */
        left: 0;
        background-color: white; /* Ensures the background is visible */
        z-index: 1000; /* Makes sure it stays on top of other content */
        padding: 10px 20px; /* Optional padding for spacing */
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1); /* Adds a subtle shadow to distinguish it from the header */
    }
</style>